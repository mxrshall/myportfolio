export default function Tag({ tag }) {
    return (
        <div className="bg-white rounded-full text-sm text-black flex justify-center items-center px-2 py-1 w-fit">
            {tag}
        </div>
    );
}
