import SectionTitle from "./SectionTitle";

export default function Contact() {
  return (
    <div 
        id="contact"
        className="w-full h-screen bg-black flex flex-col items-start justify-start relative px-10 pt-20"
    >
      <SectionTitle title="Contact." color="2" />
    </div>
  );
}
